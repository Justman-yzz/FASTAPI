# DAY 1

## ✅ DAY1 목표

1. Poetry로 가상환경/Dependencies 관리
2. FastAPI 서버 실행 및 `/docs` 확인
3. black/ruff(isort 포함) 포매터/린터 적용
4. mypy로 정적 타입 체크(strict) 실행
5. pytest로 테스트 세팅 및 실행

### 1) Poetry

“프로젝트마다 필요한 패키지와 버전”을 정리해주는 도구

`pyproject.toml`에 설정을 기록하고, `poetry.lock`으로 정확한 버전을 고정

![image.png](images/image%20(1).png)

### 결과(생성된 파일)

- `pyproject.toml` : 프로젝트 설정/의존성 목록
- `poetry.lock` : 설치된 패키지 버전이 고정된 파일

### 2) FastAPI 서버 실행

![image.png](images/image%20(2).png)

![image.png](images/image%20(3).png)

### 확인한 주소

- `http://127.0.0.1:8000/` → `{"message": "Hello World"}`
- `http://127.0.0.1:8000/docs` → Swagger 자동 문서화 화면 확인

![image.png](images/image%20(4).png)

### 3) Black / Ruff

**black**: 코드 모양을 자동으로 통일해주는 “줄맞춤 로봇(포매터)”

**ruff**: 코드에서 실수/나쁜 습관을 찾아주는 “검사기(린터)”

- 설치

poetry add --group=dev black==24.10.0 ruff==0.8.2

![image.png](images/image%20(5).png)

실수로 위의 내용을 그대로 아래에 두번 복사 붙여놓기

그래서 수정

![image.png](images/image%20(6).png)

### 결과

- ruff에서 에러가 나면 수정 후 다시 실행
- 최종적으로 `all checks passed` 확인

### 4) Mypy

mypy는 파이썬 코드를 **실행하기 전에** “타입(자료형) 관련 실수”를 찾아주는 도구

즉, 런타임 에러가 나기 전에 미리 잡아주는 “사전 검사기”

- 설치

poetry add --group=dev mypy==1.13.0

![image.png](images/image%20(7).png)

- pyproject.html(설정)에 추가

[tool.mypy]
python_version = 3.13
strict = true

![image.png](images/image%20(1).png)

### 5) Pytest

pytest는 파이썬 코드가 제대로 동작하는지 확인하는 **테스트 실행 도구**

나중에 코드를 수정해도 망가졌는지 바로 확인

- 설치

poetry add --group=dev pytest

![image.png](images/image%20(8).png)

![image.png](images/image%20(9).png)

![image.png](images/image%20(10).png)

### 결과

`1 passed` 확인